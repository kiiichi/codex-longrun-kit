[project]
name = "codex-longrun-kit"
version = "0.2.0"
description = "Codex skill for compact, reviewable long-running Codex task initialization."
requires-python = ">=3.10"

[tool.ruff]
line-length = 100
