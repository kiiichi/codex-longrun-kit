# VALIDATION_MATRIX.md

Strict-profile split file. If this file exists, it is the canonical validation map.

## Fast gate

{{FAST_VALIDATION}}

## Targeted gate

{{TARGETED_VALIDATION}}

## Full gate

{{FULL_VALIDATION}}

## Freeze gate

{{FREEZE_VALIDATION}}

## Long-running process rule

Never run dev servers in the foreground during agent work.

Use this pattern instead:

```bash
mkdir -p .codex_artifacts/logs
nohup <dev-server-command> > .codex_artifacts/logs/devserver.log 2>&1 &
echo $! > .codex_artifacts/logs/devserver.pid
# run checks
kill "$(cat .codex_artifacts/logs/devserver.pid)" || true
```

## Known flaky or expensive checks

- UNCONFIRMED.
