# VALIDATION_MATRIX.md — Validation plan

Created: {{CREATED_AT}}
Repository: {{REPO_NAME}}

## Candidate commands detected during initialization

{{VALIDATION_COMMANDS_MARKDOWN}}

## Validation levels

### Level 0 — Static sanity

Purpose: catch obvious issues quickly.

Commands:

- `git status --short`
- `git diff --stat`

### Level 1 — Narrow validation

Purpose: validate the active milestone.

Commands:

- Fill with focused test commands for the active milestone.

### Level 2 — Standard project validation

Purpose: validate normal development correctness.

Commands:

{{VALIDATION_COMMANDS_MARKDOWN}}

### Level 3 — Review freeze validation

Purpose: provide evidence for concentrated review.

Commands:

- Run relevant Level 2 commands.
- Add e2e, visual, or integration checks if applicable.

## Validation record format

When a command is run, record it in `Documentation.md`:

```text
Time:
Command:
Result: pass/fail/not-run
Evidence/log path:
Notes:
```

## Unverified commands

Commands listed here are candidates until they are actually run and pass.
